export default class Dew {
  /**
   * 
   * @param {String} name 
   * @param {String} img 
   * @param {Number} price 
   */
  constructor(name, img, price) {
    this.name = name
    this.img = img
    this.price = price
  }
}